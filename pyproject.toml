[project]
name = "count-code"
version = "0.1.0"
description = "Count total, effective, blank, and comment lines for source files."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
count-code = "count_code:main"

[build-system]
requires = ["uv_build>=0.9.0,<0.12.0"]
build-backend = "uv_build"
